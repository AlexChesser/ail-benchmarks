# ail-benchmarks
a repo designed to test ail against SWEBench pro



```
git clone https://github.com/AlexChesser/ail-benchmarks.git
cd ail-benchmarks
cp .env.example .env
# fill in two API keys & ollama server
./bootstrap.sh
./run.sh --smoke
```